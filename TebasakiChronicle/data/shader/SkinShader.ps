#version 400

//input
in vec2 tex;
in vec4 color;
in vec4 aaaa;

//output
layout(location = 0)out vec4 outputColor;

//uniform
uniform sampler2D sampler;

//FragmentShader
void main(void)
{
	outputColor = texture2D(sampler, tex);
	outputColor *= color;
	//float depth = color.z / color.w;
	//outputColor = vec4(depth, depth, depth, 1.0f);
}
